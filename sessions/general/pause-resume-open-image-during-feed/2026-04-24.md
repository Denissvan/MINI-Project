# 2026-04-24

- workshop: general
- demand track: pause-resume-open-image-during-feed
- logs analyzed:
  - user-provided timeline around `16:41:39` pause and `16:41:58` resume
- symptom:
  - During feeding, after pressing pause and then start, station 3 sends `StartFlow`/open-image before re-running updownload.
- key evidence:
  - Pause-side log shows `工站3 暂停打断上下料,恢复后必须先重新上下料`.
  - Resume-side log shows `工站3 启动测试 -> PC3 StartFlow` appears before `工站3开始上下料...`.
- root cause:
  - `myfunc.test_th()` always calls `WS.RunTestTask()` before scheduling the next updownload round.
  - `WS.RunTestTask()` only blocked `TestStatus == EMPTY/COMPLETED`, but did not block `FeedStatus == REDAYFORUPDOWNLOAD`.
  - After pause breaks updownload, the station keeps the "must updownload first" feed state, but test-thread creation still restarts testing.
- code change:
  - Added a hard guard in `UI/Class/WS.cs` `RunTestTask()`:
    - when `FeedStatus == EM_STA.REDAYFORUPDOWNLOAD`, skip creating the test thread and log the reason.
- expected result:
  - After pause/resume during feed, the station will wait for re-updownload first and will not send `StartFlow` early.
- validation:
  - code inspection only; build/run not executed in this turn
