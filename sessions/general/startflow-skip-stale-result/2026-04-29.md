# Session Note

## Basic Info

- Workshop: general
- Demand track: startflow-skip-stale-result
- Date: 2026-04-29
- Owner: Codex

## Related Logs

- Main log: `UI/bin/log/2026-04-28.log`
- DLL log: `电控端dll日志/dllforcomv8_debug.txt`
- Time window: `2026-04-28 16:23:42` to `2026-04-28 16:24:26`

## Observed Symptom

- 工站1新料 `643SL018RS / 643SL01A3Q / 643SL00UW6` 已完成上下料并打印 `PC1 StartFlow,ret=成功,sta=0`
- 随后在 `16:24:25.496` 直接收到上一轮结果 `266/0/266/0/266/0/266/0`
- 测试并未真正启动，电控误把旧结果当成新一轮结果存储

## Current Hypothesis

- 电控端 DLL 在 `StartTestFlow()` 入口发现 `result.mark != MARK_END` 时，直接 `Skip`
- 旧逻辑把 `Skip` 作为 `RES_OK` 返回，导致 C# 上层误以为新一轮 `StartFlow` 已成功
- 随后 `WaitTestResult()` 读取到的仍是上一轮残留结果

## Code Changes

- `dllforcomv8/myfunc.c`
  - 将 `StartTestFlow Skip` 日志改为 `StartTestFlow BusyLastResult`
  - 当 `result.mark != MARK_END` 时返回 `-6`，不再伪装成成功
- `UI/Class/TestPC.cs`
  - 新增 `TestPC.EM_RES.BUSY = -6`
- `UI/Class/WS.cs`
  - `WS.StartTestFlow()` 内部识别 `BUSY`
  - 命中 `BUSY` 时先等待并重试，避免直接进入后续 `WaitTestResult()`

## Validation Result

- 未编译
- 预期日志特征：
  - DLL: `StartTestFlow BusyLastResult`
  - C#: `StartFlow命中上一轮结果未闭环，等待后重试`
  - 不应再出现“`StartFlow,ret=成功` 后几秒直接收上一轮 `sta=0,n=8` 结果”

## Next Action

- 现场验证 `16:24:21` 类似场景
- 对照测试端 DLL 是否在旧结果推进到 `MARK_END` 后，C# 才真正打印新的 `StartFlow,ret=成功`
- 若仍偶发，再分析旧结果闭环过慢的根因，而不是继续放宽 `StartFlow Skip`
