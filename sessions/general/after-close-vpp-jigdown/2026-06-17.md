# After-Close VPP JigDownPhoto

- Log analyzed: `C:\Users\gdfanyd\Downloads\2026-06-16.log`, decoded with GBK.
- Demand track: `after-close-vpp-jigdown`.
- Evidence:
  - `WsModAfterClose_Shp` loaded on CamUp1 only. CamUp2 later reported `无法找到流程!名字:WsModAfterClose_Shp` but was still triggered.
  - At `19:00:00` the after-close VPP ran for module 1 targets. At `19:01:26`, later cached targets were skipped by `JigDownPhotoIntervalHours`, showing the interval was blocking later station checks inside the same timed round.
- Root cause:
  - `UpCamAfterCloseCheck(ref bool, ref WS)` did not verify that the selected upper camera had loaded `CONST.WsModAfterCloseFw`.
  - `lastJigDownPhotoTime` was used as a module-level global throttle, so the first successful run suppressed later station targets.
- Code changed:
  - `UI/Class/UpDownLoad.cs`
  - Added `HasAfterCloseVpp()` guard before per-target VPP trigger.
  - Added a round state using `jigDownPhotoRoundActive` and `jigDownPhotoCheckedKeys`.
  - Changed after-close target de-duplication from `Num` only to `WS_ID + Num`.
- Validation:
  - `git diff --check -- UI\Class\UpDownLoad.cs` exited 0.
  - Project skill says not to compile by default; build not run.
