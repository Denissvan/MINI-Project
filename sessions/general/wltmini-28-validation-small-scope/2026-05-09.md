# 2026-05-09 WLTmini 28版小范围验证改动

## Basic Info

- Workshop: general
- Demand track: wltmini-28-validation-small-scope
- Purpose: 将 28 版验证改动从当前主线断链/重连修复中拆出来，单独跟踪。
- Validation code path: `D:\MyProject\mini测试机验证\Wltmini文件对比验证27版\Wltmini文件28版本\Wltmini文件对比验证28`
- DLL strategy: 使用小米 DLL 做验证，不修改 DLL。

## Why This Track Exists

当前主线对话主要处理测试电脑断链/重连、当前 DLL 与电控复杂闭环问题。

28 版验证分支的目标不同：降低问题复杂度，回到“同排/整排 NG 不下料”的原始问题，只在电控层加小规模日志与防呆，先确认上下料模块分配和结果消费是否正确，不引入当前版本 DLL 的复杂会话逻辑。

## Key Background

- `sessions/general/full-row-ng-no-unload/2026-04-10.md` 更接近原始“同排/整排 NG 不下料”问题。
- `sessions/general/2026-05-07-test-pc-session-lost-fix.md` 是后续测试电脑断链/重连修复，不应直接混入 28 版验证。
- 2026-04-03 日志中用户指出更准确的证据链在 `13:10:18` 附近：
  - `13:09:50.365` 工站2 PC2/PC6 均返回 `sta=0,n=8`，且大量 NG。
  - `13:10:18.002` 工站2开始上下料。
  - `13:10:18.008` 上下料模块2线程开始。
  - `13:10:18.234` 上下料模块2完成退出用时 `0.063s`，疑似无实际任务或任务被过滤。
  - 上下料模块1继续运行并完成，系统最终打印 `完成工站2上下料动作...,Res1:OK,Res2:OK`。
- 当前更准确的怀疑方向：不是 DLL 结果缺失，而是电控上下料模块2未实际消费它负责区域的结果料，系统仍认为上下料成功。

## Changes Already Made In 28 Validation Folder

Modified files:

- `UI\FormView\FrMain.cs`
- `UI\Class\myfunc.cs`
- `UI\Class\UpDownLoad.cs`

### UI Version Mark

`FrMain.cs`:

- UI version display changed from:
  - `WLTmini2   V{version}`
- to:
  - `WLTmini2 验证版  V{version}`

### Downfeed Decision Trace

`myfunc.cs`:

- Added `下料判定快照` before the downfeed condition.
- Snapshot includes:
  - `bfeed`
  - `bnotfeed`
  - `wsenable`
  - `TestStatus`
  - `FeedStatus`
  - `ClearMt`
  - counts of `res == -1`, `res == -2`, `res > -1`
  - each module `Num/res/test_idx/benable/PC_ID/barcode`

### Updownload Trace And Consumption Guard

`UpDownLoad.cs`:

- Added helper snapshots:
  - `BuildWsMdSnapshot(WS ws)`
  - `BuildFeedBackSnapshot()`
  - `HasUnconsumedResultAfterUpDownload(...)`
- Added logs:
  - `线程入口快照`
  - `上下料循环快照`
  - `工站取料无任务`
  - `上下料前结果快照`
  - `上下料后结果快照`
- Added guard:
  - If both modules return `OK`, but a pre-updownload result module still exists after updownload with the same `Num + barcode + res`, log error:
    - `上下料后仍存在未消费测试结果`
  - Then set `res = EM_RES.ERR` and break from the total updownload flow.

### Compile Fix Already Applied

After first edit, compile reported:

- `CS0120`: object reference required for non-static method `UpDownLoad.HasUnconsumedResultAfterUpDownload(...)`
- Cause: total updownload flow is static context, but helper was instance method.
- Fix applied:
  - changed helper to `private static bool HasUnconsumedResultAfterUpDownload(...)`

## Important Constraints

- Do not modify DLL in this track.
- Do not bring in current version session-lost/link-lost DLL logic.
- Keep this branch focused on 28 version + Xiaomi DLL validation.
- The 28 folder already had unrelated dirty files before these changes:
  - `MotionCtrl/MyType.cs`
  - `UI/Class/Cam.cs`
  - `UI/Class/UpDownLoad.cs`
  - `UI/Class/myfunc.cs`
  - `UI/FormView/FrRun.Designer.cs`
  - `UI/FormView/FrRun.cs`
  - `UI/FormView/FrSys.Designer.cs`
  - `UI/FormView/FrSys.cs`
- Do not revert unrelated dirty changes.

## Next Validation Plan

1. Build 28 validation version.
2. Run with Xiaomi DLL.
3. Reproduce same-row/full-row NG scenario.
4. In electric log, inspect:
   - `下料判定快照`
   - `上下料前结果快照`
   - `线程入口快照`
   - `上下料循环快照`
   - `工站取料无任务`
   - `上下料后结果快照`
   - `上下料后仍存在未消费测试结果`
5. If module2 still exits in about `0.063s`, determine from logs whether:
   - `FindMod` caused no task,
   - `WsPickPos` candidate list is empty,
   - candidate positions exist but all `res < 0`,
   - result material remains unconsumed after both modules report OK.

## Prompt For New Conversation

你现在继续处理 WLTmini 28 版小范围验证分支，不要混入当前主线的测试电脑断链/重连修复。

项目主仓库：
`D:\MyProject\自动点检验证普通版本1218\自动点检验证普通版本`

28 版验证代码目录：
`D:\MyProject\mini测试机验证\Wltmini文件对比验证27版\Wltmini文件28版本\Wltmini文件对比验证28`

目标：
使用小米 DLL，不修改 DLL，只在 28 版电控层验证和修复“同排/整排 NG 不下料”问题。当前重点不是 `sta=0,n=0` 的 DLL 结果缺失，而是确认上下料模块2是否因无任务/过滤/结果消费异常导致秒退。

已有改动：
- `FrMain.cs` 版本显示改成 `WLTmini2 验证版  V...`
- `myfunc.cs` 增加 `下料判定快照`
- `UpDownLoad.cs` 增加 `线程入口快照`、`上下料循环快照`、`工站取料无任务`、`上下料前结果快照`、`上下料后结果快照`
- `UpDownLoad.cs` 增加“上下料后仍存在未消费测试结果”的校验
- `HasUnconsumedResultAfterUpDownload` 已改成 `static` 以修复 CS0120

关键历史证据：
`C:\Users\gdfanyd\Downloads\2026-04-03.log` 中 `13:09:50` 工站2 PC2/PC6 返回完整最终结果且大量 NG；`13:10:18` 工站2开始上下料，但上下料模块2线程 `0.063s` 退出，模块1继续完成，最终系统仍认为 `Res1:OK,Res2:OK`。这说明需要追踪模块2为何无实际动作，以及下料前结果是否被实际消费。

后续任务：
1. 先检查当前 28 版验证代码是否还能编译。
2. 如果有编译错误，只做最小修复。
3. 等现场验证日志后，优先分析新增日志关键字，判断模块2秒退原因。
4. 如果确认旧结果未消费，应在电控层阻止 `Res1:OK,Res2:OK` 误判，或者修正模块分配/过滤逻辑。
5. 不要引入当前版本 DLL 的 `SESSION_LOST/LINK_LOST/Clear consumed inf.status ok/BusyLastResult` 方案。

## 2026-05-09 Latest Log Analysis

Log path:

- `C:\Users\gdfanyd\Downloads\4.23\新建文件夹0509\2026-05-09.log`

Decoding:

- Read with GBK/code page 936. `Select-String -Encoding Default` produced garbled Chinese in this environment.

Version evidence:

- `10:37:17.319` shows `设备当前版本:WLTmini2 验证版  V2.4.4.28`.

New validation keyword counts in the full log:

- `线程入口快照`: 6
- `上下料循环快照`: 16
- `工站取料无任务`: 0
- `上下料前结果快照`: 3
- `上下料后结果快照`: 3
- `仍存在未消费测试结果`: 0
- `下料判定快照`: 11

Main conclusion from the validated section:

- The historical evidence happened on 工站2, but this latest现场小范围验证 intentionally enabled 工站1 only. The issue is not tied to the station number; 工站1 is a valid verification sample.
- The verified post-`10:37:17` section produced completed result/updownload cycles on 工站1.
- 工站2 snapshots at `10:43:31.824`, `10:47:17.817`, and `10:51:27.536` show `TestStatus=EMPTY`, `empty=16`, `result=0`, which is expected for the disabled/non-participating station in this run and should not be treated as evidence.
- The new full before/after result snapshots were produced for 工站1, which is the active validation station.
- In all three observed 工站1 updownload cycles, module 2 exits because it is explicitly assigned no work:
  - `FindMod1=ALL,FindMod2=NONE`
  - `上下料模块2线程入口快照,...FindMod=NONE`
  - `上下料模块2上下料循环快照,FindMod=NONE`
  - Then module 2 exits quickly.
- This maps to the current code's `PT_SET.UpDownRunMode == MD1_WORK` path in `myfunc.cs:GetRunMode()`, where `UDLoad1.FindMod = ALL` and `UDLoad2.FindMod = NONE`.
- The post-updownload guard did not trigger, and the snapshots show prior result values being consumed from `res >= 0` to `res=-2` or replaced with new untested `res=-1` material. No same `Num|barcode|res` stale-result survivor was observed in the verified section.

Implication:

- The latest validation log confirms "上下料模块2 seconds out" is caused by run-mode task allocation (`FindMod=NONE`) in the observed 工站1 verified section, not by DLL final-result loss and not by unconsumed old results.
- If the intended verification is "single active test station, but both up/down-load modules should participate", then the next focus is `PT_SET.UpDownRunMode`: it must not leave module 2 as `NONE`.
- If the intended verification is "single active test station and only up/down-load module 1 works", then module 2 quick exit is expected and the current snapshots show module 1 consumed the active station's result material.

## 2026-05-10 15:12:59-15:17:45 Log Check

Log path:

- `C:\Users\gdfanyd\Downloads\4.23\电控0511\2026-05-10.log`

Range checked:

- `15:12:59.641` through `15:17:45.504`

Conclusion:

- This range is still `WLTmini2 验证版  V2.4.4.28`.
- No confirmed "same-row/full-row NG result not unloaded" event was found in this range.
- 工站2:
  - `15:16:13.026` 下料判定快照 shows `bfeed=True`, `result=4`, modules `2/4/6/8` have `res=1`, all lower-half/PC6 positions are empty.
  - `15:16:14.915` 上下料前结果快照 shows `FindMod1=HIGH,FindMod2=LOW,resultCnt=4`.
  - `15:16:15.081/15:16:15.083` both module threads start with `FindMod=HIGH/LOW`, so module 2 is not disabled.
  - `15:16:39.307` 上下料后结果快照 shows the previous result modules no longer remain as `res=1`; active positions are replaced by `res=-1` new material with barcodes.
  - `15:16:39.308` prints `完成工站2上下料动作...,Res1:OK,Res2:OK`.
  - No `上下料后仍存在未消费测试结果` appears.
- 工站1:
  - `15:17:17.018` and `15:17:19.025` show `result=8`, `FindMod1=HIGH,FindMod2=LOW,resultCnt=8`.
  - At the requested end time `15:17:45.504`, 工站1 is still in the middle of updownload; there is no `上下料后结果快照` or `完成工站1上下料动作` within the requested range.
  - Immediately after, the log jumps to a new startup sequence around `15:19:08`, so this run segment lacks a normal 工站1 updownload completion record.

Notes:

- The `res=1` snapshots appear to be from first-cycle/close-current-test handling rather than a normal DLL final result line such as `sta=0,n=8,...`.
- For the original same-row/full-row NG issue, the strongest evidence remains a completed DLL final result (`sta=0,n=...`) followed by updownload before/after snapshots. This range does not contain that full evidence chain.
