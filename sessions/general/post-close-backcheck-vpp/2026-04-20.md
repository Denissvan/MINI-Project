# Session Note

## Basic Info

- Workshop: general
- Demand track: post-close-backcheck-vpp
- Date: 2026-04-20
- Owner: Codex

## Related Logs

- Main log: `UI/bin/log/2026-04-20.log`
- Time window: 13:24:59 to 13:25:40
- Decoding: GBK/CP936 shared-read, because the log file was in use by the running program.

## Observed Symptom

- `WsModAfterClose_Shp` starts loading at 13:25:06.814.
- It fails at 13:25:27.225 with `WsModAfterClose_Shp加载失败！未将对象引用设置到对象的实例。`
- Other camera tasks continue to load afterwards.

## Findings

- The only deployed `WsModAfterClose_Shp.vpp` found locally is under `UI/bin/product/S1A5036A+Z+M/Camera/CamUp1/`.
- `CamUp2` does not have `WsModAfterClose_Shp.vpp`.
- The new `WsModAfterClose_Shp.vpp` contains an early `Cognex.VisionPro.QuickBuild.CogJob` marker, while existing working task files such as `WsMod_Shp.vpp` start as `Cognex.VisionPro.ToolBlock.CogToolBlock`.
- The loader casts loaded objects to `CogToolBlock` and immediately dereferences `Block.GarbageCollectionEnabled`; if the VPP root object is a `CogJob`, the cast returns null and causes the observed null-reference message.

## Current Conclusion

- Primary cause: `WsModAfterClose_Shp.vpp` was likely saved/exported as a QuickBuild `CogJob` instead of a `CogToolBlock`, so the current software loader cannot load it as a normal vision task.
- Secondary issue: module/camera 2 still lacks the same task file, so even after fixing the file format for CamUp1, CamUp2 will not run this flow until its camera task directory also has the corresponding VPP.

## Next Action

- Re-save/export `WsModAfterClose_Shp.vpp` as a `CogToolBlock` VPP using the same save method/style as `WsMod_Shp.vpp`.
- Deploy the corrected file to every camera folder that needs the after-close check, especially `CamUp2` if module 2 must run it.
