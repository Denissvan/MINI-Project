# Session Note

## Basic Info

- Workshop: general
- Demand track: post-close-backcheck-vpp
- Date: 2026-04-24
- Owner: Codex

## Requirement

- Keep the existing post-place back-check unchanged.
- Add another back-check after fixture close.
- Reuse the same up-camera position.
- Use the new VPP `WsModAfterClose_Shp` and treat its `Result` as the final OK/NG for the after-close branch.

## Code Changes

- Added `UpCamAfterCloseCheck(...)` in `UI/Class/UpDownLoad.cs`.
- The new branch calls `CONST.WsModAfterCloseFw` after fixture close and uses `ResData.bOK` as the pass/fail result.
- Added the new call after `ws.AllCyClose(...)` and after `ws.SetupForTest(...)`.
- After the after-close check finishes, the up/down loader returns to the safe Y position by calling `GoZero(ref bquit, false)`.

## Validation

- Built solution with MSBuild successfully.
- Result: `0 errors`.
- Existing project warnings remain, but no new compile blocker was introduced by this change.

## Risk Reminder

- `WsModAfterClose_Shp.vpp` still must be a loadable `CogToolBlock` VPP, not a `CogJob`.
- Any up-camera directory that needs this flow must contain the same named VPP, otherwise runtime will fail to find or load the task.
